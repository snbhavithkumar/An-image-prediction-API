# An-image-prediction-API
This production-ready FastAPI image classification API uses PyTorch and ViT-B/16. Designed for scalable, low-latency inference, it features endpoints for single/batch predictions, async micro-batching, SHA-256 caching, API key auth, rate limiting, structured logging, Prometheus metrics, and Docker support.
